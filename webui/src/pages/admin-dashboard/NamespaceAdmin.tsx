import React, { FunctionComponent } from 'react';
import { Typography } from '@material-ui/core';

export const NamespaceAdmin: FunctionComponent = props => {
    return <Typography variant='h4'>Namespace Administration</Typography>;
};