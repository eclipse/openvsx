allowlist:
  # Only paths and stopwords, to test partial allowlist merging
  paths:
    - "dist/"
  stopwords:
    - "sample"

rules:
  - id: test-rule-3
    description: Third test rule with partial allowlist
    regex: "api[0-9]{3}"
    keywords:
      - api

