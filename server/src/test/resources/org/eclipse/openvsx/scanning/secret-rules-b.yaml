rules:
  - id: rule-a
    description: Override rule from file B
    regex: "override[0-9]+"
    keywords:
      - overridekey
  - id: rule-b
    description: Secondary rule from file B
    regex: "b[0-9]{2}"
    keywords:
      - key

