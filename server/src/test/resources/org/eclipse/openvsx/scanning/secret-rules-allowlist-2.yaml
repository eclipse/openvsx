allowlist:
  paths:
    - "vendor/"
    - "build/"
  regexes:
    - "^placeholder$"
    - "^dummy$"
  stopwords:
    - "fake"
    - "mock"
  file-extensions:
    - ".svg"
    - ".gif"

rules:
  - id: test-rule-2
    description: Second test rule with different allowlist
    regex: "token[0-9]{4}"
    keywords:
      - token

