rules:
  - id: rule-a
    description: Base rule from file A
    regex: "a[0-9]{3}"
    entropy: 3.5
    keywords:
      - token
      - shared
    allowlists:
      - regexes:
          - "test"
          - "sample"
    secretGroup: 1

